# Remote state backend (Section C).
# Create the S3 bucket once (bootstrap_state.sh), then:
#   terraform init -backend-config=backend.hcl

bucket       = "at-utility-tfstate"
key          = "leader/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
