# Neon BD brief — withOhm as AI Gateway beta compose peer

> **Status:** Ready after compose pack is live on Amplify + GitHub.  
> **Bryan Clark (`bryan@neon.tech`):** WAITING — do not ping until he replies.  
> **Channel for this brief:** Neon Agent Plan form + `agents@neon.tech` / `partnerships@neon.tech`.

**Audience:** Neon Agent Plan / partnerships / platform integrations (Databricks-aware).  
**Ask:** Evaluate withOhm as a complimentary compose layer beside Neon AI Gateway beta.  
**Not asking:** Logo, brand affiliation, discounted fixtures, or replacement of Neon’s gateway.

Companion: [OUTREACH_TEMPLATES.md](../OUTREACH_TEMPLATES.md) · site compose: https://www.withohm.dev/docs/compose-neon

---

## 1. Who we are

**withOhm — middleware governance.** An OpenAI-compatible AI gateway (BYOK) that rents exact-replay inventory: Redis tip isolation, Promote-on-merge, HIT meters, and signed receipts. We do not float wholesale tokens on Intermediate. We do not branch databases.

Site: https://www.withohm.dev · API: https://api.withohm.dev · Repo: https://github.com/iwasinnam2/ohm

## 2. What we noticed in AI Gateway beta

Neon’s public beta already ships **AI Gateway**: one credential, multi-provider, **branch-scoped** endpoints alongside Postgres branches. That correctly isolates *calls and state per branch*.

Agent and CI traffic still re-pays **mechanical prompt repeats** unless exact-match inventory is scoped the same way. That inventory problem is pipe rent — not a second database product.

## 3. The compliment (compose, never rival)

| Neon AI Gateway beta | withOhm upgrade |
| --- | --- |
| One API / one bill across labs | Keep — we do not replace the gateway bill story |
| Branch-scoped gateway endpoint | Pair with **exact-replay tip** on the same slug (`pr-N`) |
| Provider prompt caching (lab feature) | **Exact-match Redis inventory** + HIT meter + receipts |
| Branch lifecycle for DB + gateway | **Promote / freeze** inventory when the PR merges |
| BYOK / Databricks capacity | BYOK honesty, pre-first-byte failover, published non-goals |

**One-sentence offer:** Neon AI Gateway already branches the model path with the database; withOhm branches the exact-replay inventory and bills the HIT/MISS crossing — compose them so agent/CI previews stop re-paying mechanical prompts while Neon stays the backend center.

## 4. Why this helps Neon / Databricks

- Agent platforms on Neon stay cheaper to run on preview loops without Neon staffing a second product surface.
- Neon remains the state + gateway center; Ohm is middleware governance on repeats.
- Labs stay labs. Databricks narrative stays clean — Ohm bills the crossing, not the model relationship.
- Drop-in proof: same PR slug in CI (`templates/neon-ohm-ci`).

## 5. Proof links (ship-gated)

| Surface | URL |
| --- | --- |
| Compose docs | https://www.withohm.dev/docs/compose-neon |
| CI starter | https://github.com/iwasinnam2/ohm/tree/master/templates/neon-ohm-ci |
| Cache trees | https://www.withohm.dev/docs/cache-trees |
| Architecture | https://www.withohm.dev/product/architecture |
| Honesty map | https://www.withohm.dev/docs/honesty |

## 6. Single ask

Evaluate compose for **Agent Plan** / AI Gateway docs mention, or a short technical design-partner loop with a platform team already on Neon branches + AI Gateway.

Not asking for logo placement, affiliation language, or fixture sponsorship.

## 7. Contact

`partners@withohm.dev`

---

## Bryan hold (internal)

| Rule | Action |
| --- | --- |
| No second email to Bryan until he replies | Hold |
| If he replies | Product-peer thank-you + exec-forwardable compose note (no affiliation) |
| If right channel loops him in | Same escalate note; do not double-ping |

Exec-forwardable line (for Bryan only after reply):

> Composable exact-replay inventory for the same branch slug as Neon AI Gateway — so agent previews don’t re-tax inference while Neon stays the backend center. withOhm — middleware governance.
