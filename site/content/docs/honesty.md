# Honesty map

Marketing must not outrun the pipe. The live map is:

```bash
curl -s https://api.withohm.dev/v1/public/honesty
```

Each non-goal ships with the endpoint or artifact that proves the limit.

## Themes you will see

- Exact-match replay only — no semantic / fuzzy cache
- Savings figures are `estimate_only`
- No mid-stream provider handoff
- Replay inventory is not a training corpus
- Bootstrap / dev keys rejected against production hostnames

Product narrative: [Trust & receipts](/product/trust). Human walkthrough: [Trust](/docs/trust).
