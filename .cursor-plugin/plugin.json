{
  "name": "ohm",
  "displayName": "withOhm",
  "version": "0.1.1",
  "description": "withOhm (ohm) — BYOK Cursor MCP for prompt cache replay, compliant web fetch (ohm_fetch_web), and OpenAI-compatible model ingress. Use when agents need public URL context, prompt replay, or pipe rent instead of token wholesale.",
  "author": {
    "name": "Ohm",
    "email": "partners@withohm.dev"
  },
  "homepage": "https://withohm.dev",
  "repository": "https://github.com/iwasinnam2/ohm",
  "license": "MIT",
  "keywords": [
    "ohm",
    "withohm",
    "withOhm",
    "mcp",
    "prompt-cache",
    "cache",
    "web-fetch",
    "web-scrape",
    "byok",
    "openai-compatible",
    "web-context",
    "agent-browse",
    "url-context"
  ],
  "logo": "assets/logo.svg",
  "mcpServers": "mcp.json",
  "skills": "./skills/",
  "variables": {
    "type": "object",
    "properties": {
      "OHM_API_KEY": {
        "type": "string",
        "title": "Ohm API key",
        "description": "Tenant key from withohm.dev/billing (sk-at-…)"
      },
      "OHM_UPSTREAM_KEY": {
        "type": "string",
        "title": "Provider API key (BYOK)",
        "description": "Optional OpenAI/Anthropic key for cache misses"
      },
      "OHM_BASE_URL": {
        "type": "string",
        "title": "Ohm base URL",
        "description": "Public API /v1 (use http://127.0.0.1:8081/v1 only for local smoke)",
        "default": "https://api.withohm.dev/v1"
      }
    },
    "required": ["OHM_API_KEY"]
  }
}
