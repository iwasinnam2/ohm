# withOhm JetBrains plugin changelog

## 0.1.0

- Initial release: **Settings → Tools → withOhm**
- Registers stdio MCP server `ohm` (`ohm-mcp` + `OHM_*` env) for AI Assistant
- Stores API keys in PasswordSafe; merges into AI Assistant `mcp.json`
- Startup re-sync when enabled; Copy MCP JSON fallback notification
